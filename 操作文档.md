# OpenClaw Center 企业统一管理平台 - 操作文档

## 一、环境要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| Python | 3.11+ | 推荐使用 3.11，3.14 存在部分库兼容问题 |
| Node.js | 18+ | 前端构建和开发 |
| MySQL | 8.0+ | 数据库 |
| Git | 2.0+ | 代码管理 |

## 二、部署方式

### 2.1 Docker Compose 部署（推荐生产环境）

```bash
# 克隆仓库
git clone https://github.com/Yizhixiaomiao/openclaw-center.git
cd openclaw-center

# 启动所有服务（MySQL + 后端）
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f backend
```

启动后：
- 后端 API：http://localhost:8000
- API 文档：http://localhost:8000/docs
- 前端需单独启动（见 2.2）

### 2.2 本地开发部署

#### 2.2.1 数据库准备

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE IF NOT EXISTS openclaw_center CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 验证
SHOW DATABASES LIKE 'openclaw_center';
```

#### 2.2.2 后端启动

```bash
cd backend

# 创建 Python 虚拟环境（推荐 Python 3.11）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动后端服务
uvicorn app.main:app --reload --port 8000
```

启动成功后访问 http://localhost:8000/docs 查看 API 文档。

#### 2.2.3 前端启动

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

启动成功后访问 http://localhost:5173 。

#### 2.2.4 Agent 启动

```bash
cd agent

# 安装依赖
pip install -r requirements.txt

# 首次运行会自动创建配置文件
python -m agent
```

**首次运行交互流程**：
1. Agent 自动在 `C:\ProgramData\OpenClawCenterAgent\` 创建目录和默认 `config.yaml`
2. 提示输入中心端地址：`请输入控制中心URL: ` → 输入如 `http://192.168.1.100:8000`
3. 自动生成机器码（格式：`OC-HOSTNAME-XXXX`）并写入配置
4. 自动向控制中心注册，无需手动创建机器

**手动修改配置**（可选）：
```bash
notepad "C:\ProgramData\OpenClawCenterAgent\config.yaml"
```

## 三、配置说明

### 3.1 后端配置（backend/app/config.py）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `DATABASE_URL` | `mysql+pymysql://root:130746@localhost:3306/openclaw_center` | MySQL 连接地址 |
| `JWT_SECRET_KEY` | `openclaw-jwt-secret-change-in-production` | JWT 签名密钥，生产环境务必修改 |
| `JWT_ALGORITHM` | `HS256` | JWT 加密算法 |
| `JWT_ACCESS_TOKEN_EXPIRE_MINUTES` | `480` | Token 有效期（分钟），默认 8 小时 |
| `ENCRYPTION_KEY` | *(Fernet 密钥)* | 敏感配置加密密钥 |
| `UPLOAD_DIR` | `./uploads` | 文件上传目录（含技能包文件） |
| `CORS_ORIGINS` | `["http://localhost:5173", "http://localhost:3000"]` | 前端跨域白名单 |

**修改方式**：直接编辑 `backend/app/config.py`，或创建 `backend/.env` 文件：

```env
DATABASE_URL=mysql+pymysql://user:password@host:3306/openclaw_center
JWT_SECRET_KEY=your-production-secret-key
```

### 3.2 前端配置（frontend/vite.config.js）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 代理目标 | `http://localhost:8000` | 后端 API 地址 |
| 开发端口 | `5173` | 前端开发服务器端口 |

修改后端代理地址：

```js
server: {
  proxy: {
    '/api': {
      target: 'http://your-backend-host:8000',
      changeOrigin: true,
    },
  },
},
```

### 3.3 Agent 配置（config.yaml）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `center_url` | *(首次运行提示输入)* | 中心端地址，首次运行交互式填写 |
| `machine_code` | *(自动生成)* | 机器唯一编码，首次运行自动生成（格式：`OC-HOSTNAME-XXXX`） |
| `token` | *(空)* | 认证令牌（预留） |
| `heartbeat_interval` | `60` | 心跳上报间隔（秒） |
| `resource_interval` | `300` | 资源采集间隔（秒） |
| `config_interval` | `600` | 配置采集间隔（秒） |
| `task_interval` | `60` | 任务拉取间隔（秒） |
| `openclaw_config_path` | *(空)* | OpenClaw 配置文件路径（支持目录或文件） |
| `openclaw_skills_dir` | *(空)* | OpenClaw 技能目录 |
| `openclaw_prompts_dir` | *(空)* | OpenClaw 提示词目录 |
| `log_max_bytes` | `10485760` | 单个日志文件最大 10MB |
| `log_backup_count` | `5` | 日志保留份数 |
| `max_retries` | `3` | 通信失败重试次数 |
| `retry_delay` | `10` | 重试间隔（秒） |

### 3.4 Docker Compose 配置（docker-compose.yml）

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `MYSQL_ROOT_PASSWORD` | `openclaw_root_2024` | MySQL root 密码 |
| `MYSQL_DATABASE` | `openclaw_center` | 数据库名 |
| `MYSQL_USER` | `openclaw` | MySQL 业务用户 |
| `MYSQL_PASSWORD` | `openclaw_pass_2024` | MySQL 业务密码 |

## 四、默认账号

| 角色 | 用户名 | 密码 | 权限 |
|------|--------|------|------|
| 系统管理员 | `admin` | `admin123` | 全部权限 |

**首次登录后请立即修改密码。**

## 五、功能操作指南

### 5.1 用户管理

**路径**：侧边栏 → 用户管理

- **新增用户**：点击「新增用户」按钮，填写姓名、用户名、密码、公司、部门、岗位、角色
- **编辑用户**：点击列表中的「编辑」按钮
- **查看详情**：点击「查看」进入用户详情页，可维护职责画像、业务场景、绑定机器
- **删除用户**：点击「删除」按钮，确认后删除用户及其职责画像（不可删除自己，需 admin 角色）
- **角色说明**：
  - 系统管理员(admin)：全部权限
  - 技术支持(support)：维护场景、提示词、技能分发
  - 运维人员(ops)：查看机器状态、执行升级回滚
  - 部门负责人(manager)：查看本部门统计
  - 普通用户(user)：查看自己的场景和技能

### 5.2 机器管理

**路径**：侧边栏 → 机器管理

- **Agent 安装提示**：页面顶部显示 PowerShell 一行安装命令，可复制到目标机器执行（需管理员身份）。命令自动下载 Agent 并运行，Agent 会自注册为 Windows 计划任务实现开机自启
- **Agent 版本管理**：页面顶部显示当前 Agent 版本，点击「上传新版本」可上传新的 Agent 程序。上传后，在线 Agent 会在下次心跳时检测到新版本并自动升级
- **新增机器**：点击「新增机器」按钮，填写机器码、主机名、IP 等信息（也可通过 Agent 自动注册）
- **查看详情**：点击列表中的「查看」按钮，可查看 Agent 信息、配置摘要、已安装技能、分发记录、最近日志
- **远程访问**：如果机器配置中包含网关信息，操作栏显示「访问」按钮（多链接时显示下拉菜单），可直接跳转到 OpenClaw 控制台
- **删除机器**：点击「删除」按钮，确认后删除机器及所有关联数据（Agent 信息、配置、技能关联、日志、部署记录），需 admin 角色
- **远程配置管理**：在机器详情页的「配置」标签页可编辑 OpenClaw 配置内容，保存后自动创建同步任务推送到目标机器；在「Agent」标签页可编辑 Agent 配置（config.yaml 内容），保存后同步到 Agent
- **即时同步**：在机器详情页点击「同步」按钮，下发同步指令，Agent 会立即上报配置和技能信息
- **状态说明**：
  - 在线(online)：最近 3 分钟内有心跳
  - 离线(offline)：超过 3 分钟无心跳
  - 异常(error)：Agent 上报错误状态
  - 待初始化(pending_init)：新建未注册
  - 停用(disabled)：手动停用

### 5.3 业务场景管理

**路径**：侧边栏 → 业务场景

- 为用户创建业务场景，绑定输入描述、输出描述、处理规则
- **编辑场景**：点击「编辑」修改场景信息
- **删除场景**：点击「删除」按钮，确认后删除场景
- 状态流转：待梳理 → 已梳理 → 提示词已生成 → 技能已配置 → 测试中 → 已上线 → 需优化/暂停
- 可关联提示词模板

### 5.4 提示词模板管理

**路径**：侧边栏 → 提示词模板

- **创建模板**：填写名称、类型（通用/岗位/用户专属）、内容，支持变量占位符如 `{{用户姓名}}`、`{{岗位}}`
- **发布模板**：草稿状态 → 点击「发布」→ 已发布
- **复制模板**：点击「复制」生成新草稿，基于现有模板快速创建
- **删除模板**：点击「删除」按钮，确认后删除模板（需 admin 角色）
- **版本管理**：修改内容自动递增版本号
- **详情页**：可编辑内容、发布、回滚到指定版本

### 5.5 技能管理

**路径**：侧边栏 → 技能管理

- **新增技能**：填写技能名称、编码、版本、适用岗位、描述
- **搜索筛选**：
  - 关键词搜索支持全字段匹配（名称、编码、版本、描述、适用岗位）
  - 机器 IP 筛选为下拉选择框，自动列出所有已注册机器的 IP
- **查看详情**：点击「查看」或已安装机器数链接，打开详情对话框，包含：
  - 技能基本信息
  - 已安装机器列表（可单独移除）
  - 技能文件树浏览（从上传的技能包中提取）
  - 文件内容查看（点击文件节点即可查看内容）
- **技能分发**：点击「分发」按钮，选择目标机器列表，指定安装路径，确认后推送分发任务
- **审核流程**：待审核 → 管理员点击「通过」或「拒绝」→ 通过后自动变为已发布状态
- **删除技能**：删除技能及所有机器关联记录，同时清理服务器上的技能包文件
- **技能描述来源**：优先从 `manifest.json` 的 description 字段读取，若无则自动从 `SKILL.md` 的 YAML frontmatter 中提取

### 5.6 分发任务

**路径**：侧边栏 → 分发任务

- **创建任务**：
  1. 切换到「创建任务」标签
  2. 选择任务类型：提示词 / 技能 / 配置 / 模型配置
  3. 选择目标类型：机器 / 用户 / 部门
  4. 填写目标 ID 和 payload JSON
  5. 提交后系统自动生成每个目标机器的子任务
- **查看执行结果**：在任务列表点击「查看」，展示每台机器的执行状态
- **删除任务**：点击「删除」按钮，确认后删除任务及其所有子任务明细（需 admin 角色）
- **分页**：任务列表支持分页，可切换每页条数

**提示词分发 payload 示例**：
```json
{
  "content": "你是一个数据分析助手，请根据以下表格数据生成统计报表...",
  "target_path": "C:\\Users\\user\\.openclaw\\prompts\\report_prompt.md"
}
```

### 5.7 Coding Plan 管理

**路径**：侧边栏 → Coding Plan

- **新增 Plan**：填写服务商、套餐名称、月费用、额度类型、额度上限、预警阈值
- **绑定用户/机器**：在 Plan 详情中添加绑定关系
- **成本统计**：查看 Plan 的调用量、Token 用量、使用率、预警状态
- **删除 Plan**：点击「删除」按钮，确认后删除套餐及关联的绑定记录和用量记录（需 admin 角色）
- **预警规则**：使用率达到预警阈值（默认 80%）显示警告，达到 100% 标记高风险
- **分配建议**：
  - 轻度用户：3-5 人共用 1 个 Plan
  - 中度用户：3 人共用 1 个 Plan
  - 高频用户：1-2 人共用 1 个 Plan
  - 核心岗位：独立 Plan

### 5.8 监控日志

**路径**：侧边栏 → 监控日志

- **概览卡片**：机器总数、在线数、离线数、异常数
- **机器状态表**：所有机器的在线/离线状态、最近心跳时间
- **告警列表**：离线告警、异常告警、Plan 额度预警
- **日志查看**：按机器 ID、级别、类别筛选，支持分页

## 六、Agent 操作

### 6.1 Agent 目录结构

```
C:\ProgramData\OpenClawCenterAgent\
├── config.yaml        # 配置文件（首次运行自动创建）
├── logs\
│   └── agent.log      # 运行日志（自动轮转）
├── cache\             # 缓存目录
├── backup\            # 提示词/配置备份
├── packages\          # 技能包下载目录
└── scripts\           # 脚本目录
```

### 6.2 Agent 命令

```bash
# 启动 Agent（前台运行）
python -m agent

# 指定配置文件
python -m agent /path/to/config.yaml

# 打包为 Windows exe
pip install pyinstaller
python build.py
# 生成文件在 agent/dist/OpenClawCenterAgent.exe
```

**远程下载安装（推荐）**：在「机器管理」页面顶部，复制 PowerShell 安装命令到目标机器以管理员身份运行：

```powershell
Invoke-WebRequest -Uri "http://<中心端IP>:8000/api/agent/download" -OutFile "OpenClawCenterAgent.exe"; .\OpenClawCenterAgent.exe
```

Agent 首次运行后会自动注册为 Windows 计划任务（`OpenClawCenterAgent`），实现开机自启。

**管理计划任务**：

```powershell
# 查看计划任务状态
schtasks /query /tn "OpenClawCenterAgent"

# 手动启动
schtasks /run /tn "OpenClawCenterAgent"

# 删除计划任务
schtasks /delete /tn "OpenClawCenterAgent" /f
```

### 6.3 Agent 首次运行流程

1. **自动创建配置**：检测 `C:\ProgramData\OpenClawCenterAgent\config.yaml` 是否存在，不存在则自动生成默认配置
2. **输入中心端地址**：如果 `center_url` 未配置，交互式提示输入（如 `http://192.168.1.100:8000`）
3. **自动生成机器码**：基于主机名生成唯一编码，格式为 `OC-{主机名前12位}-{4位随机hex}`（如 `OC-WZHAOCHAOGAI-A3F2`）
4. **自动注册**：向控制中心发送注册请求，中心不存在该机器码时自动创建 Machine 记录
5. **启动后台线程**：开始周期性心跳、资源上报、配置同步、任务拉取

### 6.4 Agent 工作流程

1. 启动时执行自检（检查中心端连通性、配置完整性）
2. 向中心端注册（POST /api/agent/register）
3. 自动注册为 Windows 计划任务（开机自启）
4. 启动后台线程：
   - 心跳线程：每 60 秒上报在线状态、当前登录用户、资源使用率
   - 资源线程：每 5 分钟上报 CPU/内存/磁盘
   - 配置线程：每 10 分钟上报 OpenClaw 配置和技能列表（含全量技能包 base64 zip）
   - 技能同步线程：周期性将技能目录打包上报到中心端
   - 任务线程：每 60 秒拉取并执行分发任务
5. 任务执行：下载 → 校验 → 备份旧文件 → 写入 → 回传结果
6. 配置同步：执行配置类型任务后，如果目标路径匹配 Agent 配置路径，自动热重载配置
7. 即时同步：收到 `sync_only` 类型的配置任务时，立即上报配置和技能（不等定时周期）
8. 自动升级：心跳响应中包含新版本信息时，自动下载并替换自身可执行文件，然后重启

### 6.5 注册为 Windows 计划任务（自动）

Agent 首次运行时会自动注册为 Windows 计划任务 `OpenClawCenterAgent`，实现开机自动启动。无需手动配置。

**检查计划任务**：
```powershell
schtasks /query /tn "OpenClawCenterAgent"
```

如果需要手动注册（如计划任务被误删），以管理员身份运行：

```powershell
schtasks /create /tn "OpenClawCenterAgent" /tr "C:\path\to\OpenClawCenterAgent.exe" /sc onstart /ru SYSTEM /rl HIGHEST /f
```

**旧版方式（NSSM 服务，已弃用）**：
```bash
# 不再推荐使用 NSSM 注册为服务，请使用计划任务
nssm install OpenClawCenterAgent "C:\path\to\OpenClawCenterAgent.exe"
```

## 七、API 接口速查

### 7.1 认证接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/auth/login` | POST | 登录获取 JWT Token |

### 7.2 管理后台接口（需 JWT 认证）

| 模块 | 接口前缀 | 说明 |
|------|----------|------|
| 用户管理 | `/api/users` | 用户 CRUD、删除、职责画像 |
| 机器管理 | `/api/machines` | 机器资产、状态、详情、删除 |
| 机器配置 | `PUT /api/machines/{id}/config` | 编辑 OpenClaw 配置并创建同步任务 |
| Agent 配置 | `PUT /api/machines/{id}/agent-config` | 编辑 Agent 配置并创建同步任务 |
| 机器同步 | `POST /api/machines/{id}/sync` | 下发即时同步指令 |
| 机器 IP 列表 | `GET /api/machines/ips` | 返回去重后的机器 IP 列表 |
| 场景管理 | `/api/scenarios` | 业务场景 CRUD、删除 |
| 提示词管理 | `/api/prompts/templates` | 模板/用户提示词、版本、发布、删除 |
| 技能管理 | `/api/skills` | 技能 CRUD、审核、删除 |
| 技能详情 | `GET /api/skills/detail/{id}` | 获取技能信息及已安装机器列表 |
| 技能文件树 | `GET /api/skills/{id}/files` | 获取技能包文件树 |
| 技能文件内容 | `GET /api/skills/{id}/files/content?path=` | 获取技能包内文件内容 |
| 移除机器技能 | `DELETE /api/skills/{id}/machine/{mid}` | 从指定机器移除技能并下发删除任务 |
| 分发任务 | `/api/deploy-tasks` | 创建/查看/删除分发任务 |
| 技能分发 | `POST /api/deploy-tasks/distribute-skill` | 指定机器列表分发技能 |
| 费用管理 | `/api/coding-plans` | Plan CRUD、删除、绑定、成本统计 |
| 监控日志 | `/api/monitor` | 概览、机器状态、日志、告警 |
| Agent 下载 | `GET /api/agent/download` | 下载 Agent 可执行文件 |
| Agent 版本 | `GET /api/agent/version` | 查询当前 Agent 版本 |
| Agent 上传 | `POST /api/agent/upload` | 上传新版本 Agent（admin） |

### 7.3 Agent 接口

| 接口 | 方法 | 说明 | 请求频率 |
|------|------|------|---------|
| `/api/agent/register` | POST | Agent 注册（含当前用户、Agent 配置信息） | 首次启动 |
| `/api/agent/heartbeat` | POST | 心跳上报（含资源使用、当前用户），返回升级提示 | 每 60 秒 |
| `/api/agent/config/report` | POST | 配置摘要上报（含配置内容、技能元数据、profiles） | 每 10 分钟 |
| `/api/agent/skills/sync` | POST | 全量技能包同步（base64 zip） | 每 10 分钟 |
| `/api/agent/tasks/pull` | GET | 拉取待执行任务 | 每 60 秒 |
| `/api/agent/tasks/report` | POST | 回传任务结果 | 任务完成后 |
| `/api/agent/logs/report` | POST | 上报日志 | 按需 |
| `/api/agent/usage/report` | POST | 上报模型调用量 | 按需 |

## 八、常见问题排查

### 8.1 后端启动失败

**现象**：`uvicorn` 启动报错或立即退出

| 错误信息 | 原因 | 解决方案 |
|----------|------|---------|
| `Can't connect to MySQL server` | 数据库未启动或连接配置错误 | 检查 MySQL 服务状态，确认 `DATABASE_URL` 配置 |
| `ImportError: cannot import name 'Optional' from 'pydantic'` | Pydantic v2 不兼容 | 确保 `Optional` 从 `typing` 导入 |
| `ValueError: password cannot be longer than 72 bytes` | passlib 与新版 bcrypt 不兼容 | 使用 `bcrypt` 库替代 `passlib` |
| `pydantic-core build failed` | Python 版本不兼容 | 使用 Python 3.11 创建 venv |

### 8.2 前端页面白屏

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 登录后白屏 | API 返回数据被二次解构 | 确认 `request.js` 拦截器已返回 `response.data`，页面不再 `const { data }` |
| 页面加载空白 | 后端 API 不可达 | 检查后端是否启动，`vite.config.js` 代理配置是否正确 |
| 控制台 401 错误 | Token 过期 | 重新登录 |

### 8.3 Agent 连接失败

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| `Cannot reach center server` | 网络不通或 URL 配置错误 | 检查 `center_url`，确认 `http://host:8000/api/health` 可访问 |
| Agent 首次运行未提示输入 URL | 配置文件已存在但 center_url 为空 | 检查 `C:\ProgramData\OpenClawCenterAgent\config.yaml` 中 center_url 值，手动编辑 |
| 机器未出现在控制中心 | 注册请求未发送或机器码冲突 | 检查 Agent 日志，确认 `machine_code` 唯一性 |
| 心跳持续失败 | 防火墙阻断 | 确认中心端 8000 端口可达 |

### 8.4 数据库相关

```bash
# 查看表结构
mysql -u root -p -e "USE openclaw_center; SHOW TABLES;"

# 清空数据（保留表结构）
mysql -u root -p -e "USE openclaw_center; SET FOREIGN_KEY_CHECKS=0; SOURCE /dev/null; SET FOREIGN_KEY_CHECKS=1;"
# 或逐表清空：
mysql -u root -p -e "USE openclaw_center; TRUNCATE TABLE deploy_task_items; TRUNCATE TABLE deploy_tasks; TRUNCATE TABLE agent_logs; TRUNCATE TABLE usage_records; TRUNCATE TABLE plan_bindings; TRUNCATE TABLE coding_plans; TRUNCATE TABLE machine_skills; TRUNCATE TABLE skills; TRUNCATE TABLE user_prompts; TRUNCATE TABLE prompt_templates; TRUNCATE TABLE openclaw_configs; TRUNCATE TABLE agents; TRUNCATE TABLE business_scenarios; TRUNCATE TABLE user_profiles; TRUNCATE TABLE support_tickets; TRUNCATE TABLE machines; TRUNCATE TABLE users;"

# 重建数据库（危险操作，会丢失所有数据）
mysql -u root -p -e "DROP DATABASE openclaw_center; CREATE DATABASE openclaw_center CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
# 然后重启后端，startup 事件会自动建表和 seed 管理员
```

## 九、安全注意事项

1. **修改默认密码**：首次部署后立即修改 admin 密码
2. **修改 JWT 密钥**：生产环境务必替换 `JWT_SECRET_KEY` 为随机强密钥
3. **修改加密密钥**：替换 `ENCRYPTION_KEY` 为新的 Fernet 密钥
4. **启用 HTTPS**：生产环境建议在 Nginx 反向代理层启用 HTTPS
5. **限制 CORS**：`CORS_ORIGINS` 仅添加可信前端域名
6. **数据库访问**：生产环境不使用 root 账号，创建专用数据库用户并限制权限
7. **敏感的配置**：API Key 等敏感信息加密存储，不明文写入数据库
8. **日志脱敏**：Agent 上报日志默认不上传用户原始业务文件
